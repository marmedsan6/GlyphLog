# Versiones fijadas con == para builds reproducibles.
# Compatibles con Python 3.11+ y Python 3.14+.
# Para actualizar: evaluar el changelog, probar y fijar la nueva versión.

fastapi==0.136.3
uvicorn[standard]==0.49.0
sqlalchemy[asyncio]==2.0.41
asyncpg==0.31.0
alembic==1.18.4
pydantic==2.13.4
pydantic-settings==2.14.1
email-validator==2.2.0
# PyJWT en lugar de python-jose (CVE-2024-33663 activa en python-jose)
PyJWT==2.13.0
bcrypt==5.0.0
python-multipart==0.0.20
slowapi==0.1.10
# Librería oficial de Google para verificar ID tokens de OAuth 2.0.
# Usamos google-auth en lugar de python-jose o authlib porque es la opción
# soportada oficialmente por Google y la única que recibe parches de seguridad
# oportunos. Solo se usa para verificar tokens firmados por Google, no para
# generar ni manipular JWTs propios (esos usan PyJWT).
google-auth==2.41.1
# Dependencia transitiva de google-auth para hacer peticiones HTTP
# (necesaria para descargar los certificados de Google y verificar la firma).
requests==2.32.5
# Cliente HTTP asíncrono para consumir APIs externas como AniList e IGDB.
httpx==0.27.0
# Scraping de HowLongToBeat para el tiempo de juego (playtime) en horas.
# No tiene API oficial; la librería se envuelve en asyncio.to_thread().
howlongtobeatpy==1.0.22
# Procesamiento de imágenes para conversión de avatares a WebP.
Pillow==11.2.1
# AWS SDK para invocar modelos de Claude en Bedrock.
boto3==1.43.64
# Cliente oficial de OpenAI para el chat de GlyphAI (streaming SSE).
openai==2.53.0
# Cliente oficial de Anthropic (fallback del chat de GlyphAI).
anthropic==0.120.2
# LangChain + LangGraph para el agente de GlyphAI (tools, RAG, streaming).
# langchain-aws requiere boto3>=1.43.64 (de ahí la subida de boto3).
langchain-core==1.5.4
langchain-aws==1.2.1
langgraph==1.2.11
# Caché en memoria con TTL para APIs externas (performance optimization).
cachetools==5.5.0
# Cliente oficial de Google para YouTube Data API v3.
google-api-python-client==2.158.0
# Extracción de transcripts/subtítulos de vídeos de YouTube sin API key.
youtube-transcript-api==0.6.3

