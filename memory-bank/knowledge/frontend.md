# Conocimiento Frontend — GlyphLog

> Conocimiento acumulado sobre la capa frontend del proyecto (`apps/web`).
> Este archivo se irá completando durante el desarrollo. Por ahora es un placeholder estructurado.
> Actualizar al resolver problemas técnicos relevantes o establecer patrones específicos de frontend.

---

## Decisiones de setup

> Estado: pendiente de scaffold de `apps/web`.

Cuando se complete el scaffold, documentar aquí:

- Versión exacta de Vite y plugins configurados
- Configuración de TypeScript (`tsconfig.json` — paths, strict mode, etc.)
- Configuración de Tailwind (`tailwind.config.ts` — colores, fuentes, breakpoints)
- Configuración de shadcn/ui (tema elegido, estilo base)
- Configuración de React Router (versión, estructura de rutas)
- Configuración de alias de imports (`@/` → `src/`)
- Herramientas de linting y formatting (ESLint, Prettier)

---

## Componentes creados

> Estado: ninguno todavía. El scaffold de `apps/web` está pendiente.

Cuando se creen componentes, documentar aquí los no triviales:

```
| Componente | Ruta | Descripción | Props principales |
|------------|------|-------------|-------------------|
| —          | —    | —           | —                 |
```

---

## Hooks creados

> Estado: ninguno todavía.

Cuando se creen hooks, documentar aquí los relevantes:

```
| Hook | Ruta | Descripción | Retorna |
|------|------|-------------|---------|
| —    | —    | —           | —       |
```

---

## Problemas conocidos

> Estado: ninguno todavía.

Cuando se encuentren problemas técnicos en el frontend (bugs de librerías, limitaciones, workarounds), documentarlos aquí con su solución o estado.

---

## Tips y aprendizajes

> Estado: vacío. Se irá completando durante el desarrollo.

Ejemplos de lo que se documenta aquí:

- Cómo configurar correctamente el proxy de Vite para apuntar a la API local
- Cómo manejar correctamente los estados de carga en formularios con shadcn/ui
- Qué componentes de shadcn/ui requieren configuración adicional de accesibilidad
- Cómo integrar React Router con el layout raíz

---

*Este archivo se irá completando durante el desarrollo. Actualizarlo al finalizar cada sesión de trabajo relevante para el frontend.*
